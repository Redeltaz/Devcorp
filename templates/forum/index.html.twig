{% extends 'base.html.twig' %}

{% block title %}
    Forum
{% endblock %}
{% block stylesheets %}
    <link href="{{ asset("css/forum/index.css") }}" rel="stylesheet">
{% endblock %}

{% block body %}
    <h1 class="title">Bienvenue sur le forum</h1>
    <a href="{{ path("forum_create") }}">Créer un poste</a>

    {% for elem in postes %}
        <h1>{{ elem.title }}</h1>
        <p>{{ elem.content }}</p>
    {% endfor %}
{% endblock %}
